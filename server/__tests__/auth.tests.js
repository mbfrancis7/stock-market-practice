const { signup, login, setToken, verifyToken } = require('../auth/auth.controllers')
const jwt = require('jsonwebtoken')

describe('authentication' () => {
  describe('signup', () => {
    test('takes valid email password combination', () => {
      
    }) 
  })
})